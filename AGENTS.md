# Development Environment

- Use `flake.nix` to track every system dependency required to develop, test, or run this project.
- Do not install project dependencies into systemwide package locations.
- Enter the development environment with `nix develop`, or run commands with `nix develop --command <command>`.
